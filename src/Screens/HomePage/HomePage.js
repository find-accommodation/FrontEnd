import React from "react";
import { ScrollView, Text } from "react-native";

const HomePage = () => {
  return (
    <ScrollView>
      <Text>HomePage</Text>
    </ScrollView>
  );
};

export default HomePage;
